import { Container } from '@shared/components/container';

import { CreateProposal } from '../../components/screens/createProposal';

export default function Page() {
  return (
    <Container>
      <CreateProposal />
    </Container>
  );
}
